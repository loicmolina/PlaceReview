<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="ProjetDevMobile.Views.DetailsReviewPage">
    <ContentPage.Resources>
        <ResourceDictionary>
            <Style TargetType="Label">
                <Setter Property="FontSize" Value="Large" />
                <Setter Property="TextColor" Value="DarkGray"/>
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>
    <ContentPage.Content>
        <StackLayout Spacing="20">
            <Image Source="{Binding Review.Photo.Source}" HorizontalOptions="Center" HeightRequest="800" WidthRequest="800"/>
            <Label Text="{Binding Review.Titre}" FontAttributes="Bold"/>
            <Label Text="{Binding Review.Description}"/>
            <Label Text="{Binding Review.Tag}" FontAttributes="Italic" />
        </StackLayout>
    </ContentPage.Content>
  
</ContentPage>